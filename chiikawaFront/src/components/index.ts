/**
 * 这个文件作为组件的目录
 * 目的是统一管理对外输出的组件，方便分类
 */

// 布局组件
import Footer from './Footer'

// 头部工具组件
import { Question, SelectLang } from './HeaderTools'

// 用户相关组件
import { AvatarDropdown, AvatarName } from './AvatarDropdown'

export { AvatarDropdown, AvatarName, Footer, Question, SelectLang }
